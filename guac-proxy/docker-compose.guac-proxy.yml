version: "3.8"

services:
  guac-proxy:
    image: nginx:alpine
    container_name: guac-proxy
    volumes:
      - ./nginx/guac.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - interna

networks:
  interna:
    external: true   # reutiliza a mesma rede 'interna' do stack Nextcloud

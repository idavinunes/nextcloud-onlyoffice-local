services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.nc
      args:
        BASE_IMAGE: ${NEXTCLOUD_IMAGE:-nextcloud:31-apache}
    image: nextcloud-custom:latest

  cron:
    build:
      context: .
      dockerfile: Dockerfile.nc
      args:
        BASE_IMAGE: ${NEXTCLOUD_IMAGE:-nextcloud:31-apache}
    image: nextcloud-custom:latest
